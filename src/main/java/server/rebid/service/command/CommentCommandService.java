package server.rebid.service.command;

import server.rebid.dto.response.CommentResponse;

public class CommentCommandService {
    public CommentResponse.CommentId addComment(Long materialId, String content) {
    }
}
